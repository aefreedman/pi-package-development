---
description: Assess and prepare a Pi package for release without publishing it
argument-hint: "<package-path>"
---

Use the `preparing-pi-package-releases` skill to assess and prepare `${1:-the package in the current working directory}`.

Do not publish, push, create a release, or change the package version unless explicitly authorized. Report blockers, validation evidence, unresolved decisions, and the exact next release step.
