# Synthetic conformance fixtures

Downstream package authors import `@example/conformance-fixtures/fixtures` to validate their implementations against the supported public conformance dataset. The exported fixtures are versioned consumer API.
