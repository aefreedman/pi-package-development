# Synthetic audit scope fixture

`target-package` is the only requested package. `sibling-package` contains a sentinel that must not be read by the explicit-scope and package-status negative-control cases. The fixture is synthetic and contains no credentials, private project data, generated output, or Git metadata.
